# 依赖说明（升级日期 2026-09-05）：
# 1. pysocks：用于 urllib / socks5 代理支持
# 2. curl-cffi：用于 Chrome TLS 指纹模拟，绕过 WAF/安域防护/Cloudflare 挑战
#    （吾爱破解、NodeSeek、AgentRouter 等）。固定 0.16.3（实测 Chrome124 指纹绕过阿里 WAF）。
# 3. pycryptodome：用于移动端协议加密（如 SMZDM DES-ECB 计算）。
# 精确锁定以复现 CI/本地一致环境，便于排查。
pysocks==1.7.1
curl-cffi==0.16.3
pycryptodome==3.23.0