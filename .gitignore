# Local secrets / env files
.env
.env.*
.secrets-*
!.env.example

# Local Claude Code state
.claude/

# Archived legacy QD framework; kept locally, not deployed or committed by default
qd-framework-archive/

# Runtime/cache files
__pycache__/
*/__pycache__/
*.pyc
*.pyo
*.log
.DS_Store
docs

# WSL2 metadata
*:Zone.Identifier

# SSH keys (never commit)
id_ed25519
id_ed25519.pub

# Test fixtures / local backups (never commit)
test_data/
*.enc
*.db

# Markdown docs (internal notes, not for public/deploy)
*.md
!README.md
!CLAUDE.md
